name: vlm_node_1016
channels:
  - defaults
dependencies:
  - python=3.10.16
  - pip
  - pip:
    # Core PyTorch (CUDA 12.1)
    - torch==2.8.0+cu126
    - torchvision==0.23.0+cu126
    - torchaudio==2.8.0+cu126
    - --extra-index-url https://download.pytorch.org/whl/cu126
    
    # Build tools for Flash Attn
    - packaging
    - ninja
    
    # VLM Essentials
    - transformers==4.57.3
    - accelerate==1.9.0
    - flash_attn==2.8.3
    - bitsandbytes
    - peft
    - datasets
    # - deepspeed  # Optional: For multi-node or ZeRO stage optimization
    - wandb      # Optional: For logging
    - ray[default]

    # VERL dependencies
    